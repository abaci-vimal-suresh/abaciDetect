# Authentication Configuration

# Auth Strategy: 'httponly' (recommended) or 'js-cookie'
# - httponly: Backend sets HttpOnly cookie, frontend uses withCredentials: true
# - js-cookie: Backend returns token, frontend stores in JS-accessible cookie
VITE_AUTH_STRATEGY=httponly

# Use Mock Authentication (for development without backend)
# Set to 'true' to use mock service, 'false' to use real API
VITE_USE_MOCK_AUTH=false

# Site Name
VITE_SITE_NAME=HALO Sensor Management

# Backend URL is configured in src/helpers/baseURL.js
