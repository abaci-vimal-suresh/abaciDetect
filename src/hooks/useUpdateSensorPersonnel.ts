// Add these hooks to your existing sensorService.ts file

// --- Personnel Management APIs ---

// Mock personnel data for each sensor
const mockPersonnelData: { [sensorId: string]: { name: string; contact: string; email: string } } = {};

;